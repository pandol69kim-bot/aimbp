# 작업내역: 작곡 실패 사유 확인 기능 추가

**날짜:** 2026-06-17 19:45  
**작업자:** Claude Code

## 작업 내용

작곡 생성 실패 시 상세한 에러 메시지를 저장하고 조회할 수 있는 기능 추가

## 구현 내용

### 1. Backend 데이터베이스 변경

#### Track 모델에 error_message 필드 추가
```python
# backend/app/models/track.py
error_message: Mapped[str | None] = mapped_column(String(1024), nullable=True)
```

#### 데이터베이스 컬럼 생성
```sql
ALTER TABLE tracks ADD COLUMN IF NOT EXISTS error_message VARCHAR(1024);
```

### 2. API 응답 스키마 업데이트

#### TrackResponse
```python
class TrackResponse(BaseModel):
    ...
    error_message: Optional[str] = None
    ...
```

#### TrackStatusResponse
```python
class TrackStatusResponse(BaseModel):
    status: str
    file_url: Optional[str] = None
    task_id: Optional[str] = None
    error_message: Optional[str] = None  # 추가됨
```

### 3. Backend 서비스 로직 업데이트

#### Suno API 에러 저장
```python
except Exception as e:
    error_msg = f"Suno API failed: {str(e)}"
    # error_message를 Track에 저장
    db_track.error_message = error_msg
    # Mock MP3로 폴백
    await _mock_generation(...)
```

#### Mureka API 에러 저장 (상세)
```python
except httpx.HTTPStatusError as e:
    error_detail = f"HTTP {e.response.status_code}"
    # JSON 파싱 후 에러 메시지 추출
    if "error" in error_json:
        error_detail = f"{error_detail}: {error_json['error']['message']}"
    # Track에 저장
    db_track.error_message = error_detail
```

### 4. Frontend UI 업데이트

#### error_message 표시 (TrackItem)
```typescript
{displayTrack.error_message && (
  <p className="text-xs text-red-400 mt-2 bg-red-500/10 px-2 py-1 rounded">
    ❌ {displayTrack.error_message}
  </p>
)}
```

#### Track 타입 업데이트
```typescript
export interface Track {
  ...
  error_message?: string  // 추가됨
}
```

## API 응답 예시

### 성공한 곡 생성
```json
{
  "success": true,
  "data": {
    "id": "...",
    "status": "completed",
    "file_url": "https://...",
    "error_message": null
  }
}
```

### 실패한 곡 생성 (상태 조회)
```json
{
  "success": true,
  "data": {
    "status": "failed",
    "file_url": null,
    "task_id": "...",
    "error_message": "Mureka API failed: HTTP 429: You exceeded your current quota, please check your plan and billing details"
  }
}
```

### 실패한 곡 생성 (상세 조회)
```json
{
  "success": true,
  "data": {
    "id": "46a08a59-a15f-4c81-9ba7-c9817f359c29",
    "status": "failed",
    "file_url": null,
    "error_message": "Mureka API failed: HTTP 429: You exceeded your current quota, please check your plan and billing details",
    "created_at": "2026-06-17T07:54:02.438348+00:00"
  }
}
```

## 에러 메시지 종류

### Suno API 에러
```
❌ Suno API failed: Server error '503 Service Unavailable'
```

### Mureka API 에러 (할당량 초과)
```
❌ Mureka API failed: HTTP 429: You exceeded your current quota, please check your plan and billing details
```

### Mureka API 에러 (가사 누락)
```
❌ Mureka API failed: HTTP 400: Invalid Request, The lyrics are empty.
```

### 일반 에러
```
❌ API 호출 실패: [구체적인 에러 메시지]
```

## Frontend 표시 방식

### 곡 목록 (music/page.tsx)
```
┌─────────────────────────────────────┐
│ 곡제목  [failed]                     │
│ Genre · 2026-06-17                  │
│ ❌ HTTP 429: You exceeded your...    │
│ [오디오플레이어] [다운로드]          │
└─────────────────────────────────────┘
```

### 색상 및 스타일
- 배경: `bg-red-500/10` (밝은 빨강)
- 텍스트: `text-red-400` (빨강)
- 아이콘: ❌ (에러 기호)
- 여백: `mt-2 px-2 py-1 rounded`

## 테스트 결과

### 테스트 시나리오
```
1. Mureka AI 선택
2. 곡 생성 요청
3. API 할당량 초과 (HTTP 429)
4. Mock MP3로 폴백 (사용자는 모르게)
5. error_message 저장
```

### 결과
```
Status: "failed"
file_url: null
error_message: "Mureka API failed: HTTP 429: You exceeded your current quota..."

Frontend 표시: ✅ 에러 메시지 보임
```

## 수정된 파일

| 파일 | 변경 내용 |
|------|---------|
| `backend/app/models/track.py` | error_message 필드 추가 |
| `backend/app/schemas/track.py` | TrackResponse, TrackStatusResponse 업데이트 |
| `backend/app/services/ai_music.py` | 에러 메시지 저장 로직 추가 |
| `backend/app/api/music.py` | status 엔드포인트 error_message 포함 |
| `frontend/src/types/index.ts` | Track 인터페이스 업데이트 |
| `frontend/src/app/(dashboard)/music/page.tsx` | 에러 메시지 UI 추가 |

## API 엔드포인트

### 상태 조회 (빠른 확인)
```bash
GET /api/v1/music/{track_id}/status
# 응답: status, file_url, error_message
```

### 상세 조회 (전체 정보)
```bash
GET /api/v1/music/{track_id}
# 응답: 전체 Track 정보 포함 error_message
```

### 곡 목록 (모든 곡)
```bash
GET /api/v1/music
# 응답: 모든 Track 배열 (각각 error_message 포함)
```

## 사용 방법 (사용자)

### Frontend에서 에러 확인
1. http://localhost:3001 접속
2. 작곡 메뉴
3. 곡 생성 후 실패 시
4. ❌ 빨간색 에러 메시지 표시
5. 메시지 읽고 원인 파악

### 예시
```
❌ HTTP 429: You exceeded your current quota, please check your plan and billing details

→ Mureka API 할당량 초과 (유료 플랜 필요)
```

## 향후 개선사항

### 에러 메시지 한국어화
```python
ERROR_MESSAGES = {
    "429": "API 할당량을 초과했습니다. 유료 플랜을 확인해주세요.",
    "503": "음악 생성 서비스가 일시적으로 이용 불가능합니다.",
    "400": "입력 값이 올바르지 않습니다.",
}
```

### 재시도 버튼
```typescript
<button>
  🔄 다시 시도
  {/* 다른 AI 서비스로 재시도 */}
</button>
```

### 상세 에러 로그
```python
# 시간, IP, 사용자, API 응답 전체 저장
class ErrorLog:
  timestamp: datetime
  user_id: UUID
  track_id: UUID
  api_service: str
  http_status: int
  error_message: str
  full_response: str
```

## 기술 스택

### Backend
```python
# SQLAlchemy ORM
error_message: Mapped[str | None] = mapped_column(String(1024))

# Pydantic v2
error_message: Optional[str] = None

# FastAPI
return TrackStatusResponse(..., error_message=track.error_message)
```

### Frontend
```typescript
// React Hook
{displayTrack.error_message && (
  <p className="text-red-400">❌ {displayTrack.error_message}</p>
)}

// Tailwind CSS
className="text-red-400 bg-red-500/10 px-2 py-1 rounded"
```

## 로그 분석

### Backend 로그 예시
```
2026-06-17 07:54:03,479 - app.services.ai_music - ERROR - Mureka API error: HTTP 429: You exceeded your current quota, please check your plan and billing details
2026-06-17 07:54:03,479 - app.services.ai_music - ERROR - Mureka generation error for 46a08a59-a15f-4c81-9ba7-c9817f359c29: Mureka API failed: HTTP 429: You exceeded your current quota, please check your plan and billing details
2026-06-17 07:54:03,494 - sqlalchemy.engine.Engine - INFO - UPDATE tracks SET status=$1::VARCHAR, error_message=$2::VARCHAR
```

### 데이터베이스 저장 확인
```sql
SELECT id, status, error_message FROM tracks WHERE status = 'failed';

id                                   | status | error_message
46a08a59-a15f-4c81-9ba7-c9817f359c29 | failed | Mureka API failed: HTTP 429: You exceeded your current quota...
```

## 검증 체크리스트

- [x] error_message 필드 추가
- [x] Track 모델 수정
- [x] 데이터베이스 컬럼 생성
- [x] API 응답 스키마 업데이트
- [x] Suno 에러 저장
- [x] Mureka 에러 저장 (상세 메시지)
- [x] Status 엔드포인트 업데이트
- [x] Frontend UI 추가
- [x] Frontend 타입 업데이트
- [x] 테스트 완료 ✅

## 상태 요약

| 항목 | 상태 |
|------|------|
| **Database** | ✅ error_message 컬럼 추가됨 |
| **API** | ✅ 에러 메시지 반환 |
| **Frontend** | ✅ 에러 메시지 표시 |
| **테스트** | ✅ Mureka 429 에러 확인됨 |
| **사용 가능** | ✅ 즉시 사용 가능 |

---

**최종 결과**: 작곡 생성 실패 시 원인을 명확히 파악할 수 있습니다! 🎵
