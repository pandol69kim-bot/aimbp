# 작업 내역

- **날짜**: 2026-06-17 12:00
- **작업자**: Claude

## 작업 내용

AIMBP Phase 1·2 풀스택 프로젝트 구현 및 Docker dev 환경 구동 완료.

## 변경된 파일

### 신규 생성 (백엔드 - 47개 파일)
- `docker-compose.yml` — DB/Backend/Frontend 3서비스 구성
- `.env.example` / `.env` — 환경변수 템플릿
- `backend/Dockerfile` — python:3.11-slim 기반
- `backend/requirements.txt` — FastAPI, SQLAlchemy, passlib, openai, anthropic 등
- `backend/alembic.ini` + `backend/alembic/env.py` — async 마이그레이션
- `backend/app/main.py` — FastAPI 앱, CORS, lifespan, 라우터 등록
- `backend/app/core/config.py` — pydantic-settings 환경변수
- `backend/app/core/database.py` — async SQLAlchemy 엔진 + init_db
- `backend/app/core/security.py` — JWT 토큰, bcrypt, get_current_user
- `backend/app/models/` — User, Lyrics, Track, Album, Arrangement, Vocal, Mastering, Cover
- `backend/app/schemas/` — Pydantic v2 스키마 (Create/Response)
- `backend/app/api/` — auth, lyrics, music, files, albums, arrangement, vocal, mastering, cover
- `backend/app/services/` — ai_lyrics, ai_music, s3, arrangement, vocal, mastering, cover

### 신규 생성 (프론트엔드 - 45개 파일)
- `frontend/package.json` — Next.js 14, React Query, Zustand, axios, Tailwind
- `frontend/next.config.js` — Next.js 설정 (next.config.ts → .js 변환)
- `frontend/tailwind.config.ts` — 다크 테마 커스텀 색상
- `frontend/src/app/` — 인증 페이지(login/register) + 대시보드 8개 페이지
- `frontend/src/components/` — UI 컴포넌트, 레이아웃, AudioPlayer
- `frontend/src/lib/api.ts` — axios 인스턴스 (JWT 인터셉터)
- `frontend/src/stores/authStore.ts` — Zustand + localStorage persist
- `frontend/src/hooks/` — useAuth, useLyrics, useMusic, useAlbums

### 수정
- `docker-compose.yml`: version 속성 제거, 포트 8001/3001, frontend_node_modules 볼륨 추가
- `backend/requirements.txt`: `pydantic[email]`, `email-validator`, `bcrypt==4.0.1` 추가
- `frontend/src/app/globals.css`: `@apply border-border` → `border-color: rgba()` 직접 CSS로 변경
- `frontend/src/lib/api.ts`: baseURL 8000 → 8001

## 주요 결정 사항

| 항목 | 결정 | 이유 |
|------|------|------|
| AI API 없을 때 | Mock 데이터 자동 반환 | dev 환경에서 API 키 없이도 동작 |
| S3 없을 때 | 로컬 `/app/uploads` fallback | dev 환경 독립성 |
| 포트 변경 | 8000→8001, 3000→3001 | 기존 프로세스 충돌 방지 |
| bcrypt 버전 | `bcrypt==4.0.1` 고정 | passlib 1.7.4와 호환 이슈 |
| next.config.ts | next.config.js로 변환 | Next.js 14는 .ts 설정 파일 미지원 |

## 실행 중인 서비스

| 서비스 | 포트 | 상태 |
|--------|------|------|
| PostgreSQL | 5432 | healthy |
| FastAPI Backend | 8001 | running |
| Next.js Frontend | 3001 | running |

## 검증 결과

- `GET http://localhost:8001/health` → `200 OK`
- `POST /api/v1/auth/register` → 회원가입 성공
- `POST /api/v1/auth/login` → JWT 토큰 발급 성공
- `GET /api/v1/auth/me` → 사용자 정보 반환 성공
- `POST /api/v1/lyrics/generate` → Mock 작사 생성 성공
- `GET http://localhost:3001` → `307 /login` 리디렉션 정상

## 작업 커맨드 로그

- `12:00` : `docker compose build backend` → 백엔드 이미지 빌드
- `12:05` : `docker compose up -d backend` → 포트 충돌 수정 후 기동
- `12:10` : `pydantic[email]`, `bcrypt==4.0.1` 추가 후 재빌드
- `12:15` : `docker compose up -d frontend` → 프론트엔드 기동
- `12:20` : `next.config.ts → .js` 변환, `globals.css` border-border 수정
- `12:25` : `.next` 캐시 삭제 후 재시작 → 전체 정상 동작 확인
